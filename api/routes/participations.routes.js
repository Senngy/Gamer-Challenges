// participations.routes.js
import express from 'express';

const router = express.Router();

router.post('/',) // Create participations PRIVATE
router.delete('/:id') // delete participations PRIVATE


export default router;